# This project currently uses only Python's standard libraries.
# No external packages are required to run the application.

# If you add any dependencies in the future (e.g., using pip),
# you can generate an updated list by running:
# pip freeze > requirements.txt
